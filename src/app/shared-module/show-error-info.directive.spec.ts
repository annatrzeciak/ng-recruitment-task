import { ShowErrorInfoDirective } from './show-error-info.directive';

describe('ShowErrorInfoDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowErrorInfoDirective();
    expect(directive).toBeTruthy();
  });
});
