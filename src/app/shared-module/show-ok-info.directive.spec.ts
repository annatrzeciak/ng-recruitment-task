import { ShowOKInfoDirective } from "./show-ok-info.directive";

describe('ShowOKInfoDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowOKInfoDirective();
    expect(directive).toBeTruthy();
  });
});
